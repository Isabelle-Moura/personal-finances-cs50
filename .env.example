# ----------------------------------------------------------------------------------
# .env.example
# 
# This file serves as a template for environment variables.
# Create a file called “.env” with these variables and fill in your actual values.
# The “.env” file MUST NOT be committed to Git for security reasons.
# ----------------------------------------------------------------------------------

# Flask's environment variables
# Defines how Flask should run the application

# Tells Flask which file it should execute to start the application.
FLASK_APP=run.py
# Puts Flask in development mode. This activates the debugger and automatic reloader.
FLASK_ENV=development

# SENSITIVE Environment Variables (Secrets)

# Secret key for Flask session security
# Generate a long, random string
SECRET_KEY=‘your_secret_key_here_for_session_security’

# PostgreSQL Database Settings (CRITICAL!)
# Change user and password to your actual PostgreSQL credentials
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL=‘postgresql://database_user:database_password@localhost:5432/your_database_name’

# Flask uses this to define which port the server should run on.
# The value 5000 is the default for local development.
PORT=3000