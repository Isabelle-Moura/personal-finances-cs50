{
    "name": "personal-finances-frontend",
    "version": "1.0.0",
    "description": "Front-end's dependencies for Flask's application.",
    "dependencies": {
      "bootstrap": "^5.3.3",
      "chart.js": "^4.4.3",
      "@fortawesome/fontawesome-free": "^6.5.2"
    },
    "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "activate-venv": "echo To ENABLE the virtual environment, use one of the commands bellow: && echo ------------------------------------------------------- && echo At Git Bash/Linux/Mac: source venv/Scripts/activate && echo At Windowss CMD: venv\\Scripts\\activate && echo -------------------------------------------------------",
    "start": "echo WARNING ❗ Ensure that the virtual environment (venv) is ENABLED before running this command. && python run.py"
  }
  }